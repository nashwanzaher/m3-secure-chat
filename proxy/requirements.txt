# M3 Secure Proxy — runtime dependencies (Phase 1)
# Pin minor versions so production builds are reproducible.
fastapi==0.136.3
uvicorn[standard]==0.30.6
httpx==0.27.2
pydantic==2.9.2

# Structured logging (JSON in production, pretty in dev).
structlog==24.4.0

# Optional: error reporting. The proxy starts even if Sentry is not configured.
# Sentry SDK is imported lazily in main.py.
sentry-sdk[fastapi]==2.19.2

# Optional: distributed tracing. Imported lazily in main.py.
opentelemetry-api==1.28.2
opentelemetry-sdk==1.28.2
opentelemetry-instrumentation-fastapi==0.49b2
opentelemetry-instrumentation-httpx==0.49b2
opentelemetry-exporter-otlp-proto-grpc==1.28.2
opentelemetry-semantic-conventions==0.49b2
