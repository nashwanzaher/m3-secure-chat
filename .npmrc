store-dir=/tmp/.pnpm-store
virtual-store-dir=/tmp/m3-secure-chat/.pnpm
# Hoist vitest so @testing-library/jest-dom/vitest can resolve it
# (jest-dom 6.9.x does a bare `import 'vitest'` from inside its own subfolder).
public-hoist-pattern[]=*vitest*
